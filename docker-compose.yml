version: "3.8"

services:
  medifusion-api:
    build: .
    container_name: medifusion-ai
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
      - ./reports:/app/reports
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
